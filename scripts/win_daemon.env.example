# Windows 守护进程配置（复制为 win_daemon.env，填实际值，勿入库）
# Linux 后端地址（同区域内网IP 或 公网EIP）
BACKEND_URL=http://<linux内网IP>
# 后端 admin 账号
SERVICE_USER=admin
SERVICE_PASSWORD=<后端admin密码>
# webhook 共享密钥（与后端 .env 的 WEBHOOK_SECRET 完全一致）
WEBHOOK_SECRET=<与后端一致的密钥>
